import { InjectionToken } from '@angular/core';
import { WindowService } from './window.service';
export interface RtPlatformConfig {
    /** usually saved in settings */
    localBaseUrl?: string;
    /** have lower priority, as build may be run on a local machine */
    serverBaseUrl?: string;
}
export declare function factoryFn(windowService: WindowService): Window;
export declare const RtPlatformConfigToken: InjectionToken<RtPlatformConfig>;
export declare const defaultConfig: RtPlatformConfig;
