/**
 * CSS font style rule.
 * May be extracted from the host element by calling `window.getComputedStyle(hostElement).getPropertyValue('font')`.
 * See: https://developer.mozilla.org/en-US/docs/Web/CSS/font
 */
export type CSSFont = string;
